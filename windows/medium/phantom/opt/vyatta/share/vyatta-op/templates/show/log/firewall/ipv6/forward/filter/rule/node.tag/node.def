help: Show log for a rule in the specified firewall
allowed: /bin/cli-shell-api listActiveNodes firewall ipv6 forward filter rule | sed -e "s/'//g" && echo
run: journalctl --no-hostname --boot -k | egrep "\[ipv6-FWD-filter-$8-[ADRJCO]\]"
