#!/bin/sh

set -e

invoke-rc.d isc-dhcp-relay stop

# Automatically added by dh_installinit/13.11.4
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/isc-dhcp-relay" ] ; then
	invoke-rc.d --skip-systemd-native isc-dhcp-relay stop || exit 1
fi
# End automatically added section


exit 0
