#!/bin/sh
set -e
# Automatically added by dh_installinit/13.11.1~bpo11+1
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/salt-minion" ] ; then
	invoke-rc.d salt-minion stop || exit 1
fi
# End automatically added section
