#!/bin/sh
set -e
# Automatically added by dh_installinit/13.10
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/tftpd-hpa" ] ; then
	invoke-rc.d tftpd-hpa stop || exit 1
fi
# End automatically added section
