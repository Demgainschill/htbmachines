#!/bin/sh
set -e
# Automatically added by dh_installinit/13.11.4
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/twamp-server" ] ; then
	invoke-rc.d twamp-server stop || exit 1
fi
# End automatically added section
