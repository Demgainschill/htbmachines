help: Show log for a rule in the specified firewall
allowed: /bin/cli-shell-api listActiveNodes firewall ipv4 output filter rule | sed -e "s/'//g" && echo
run: journalctl --no-hostname --boot -k | egrep "\[ipv4-OUT-filter-$8-[ADRJC]\]"
