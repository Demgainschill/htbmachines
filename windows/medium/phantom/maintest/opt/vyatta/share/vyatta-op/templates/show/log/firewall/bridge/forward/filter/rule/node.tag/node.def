help: Show log for a rule in the specified firewall
allowed: /bin/cli-shell-api listActiveNodes firewall bridge forward filter rule | sed -e "s/'//g" && echo
run: journalctl --no-hostname --boot -k | egrep "\[bri-FWD-filter-$8-[ADRJC]\]"
